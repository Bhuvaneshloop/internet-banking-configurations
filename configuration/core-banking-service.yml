spring:
  datasource:
    url: jdbc:mysql://192.168.1.24:3306/banking_core_service
    username: dell
    password: P3S0luTions!
  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 8092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

info:
  app:
    name: ${spring.application.name}

##ACTUATOR ENDPOINT CONFIGURATIONS
management:
  endpoints:
    web:
      exposure:
        include: info,health,configprops
  info:
    env:
      enabled: true

logging:
  file:
    name: '/var/log/core-banking-service/application.log'
